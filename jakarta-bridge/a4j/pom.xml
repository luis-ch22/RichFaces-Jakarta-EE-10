<?xml version="1.0" encoding="UTF-8"?>
<!--
  Jakarta-transformed variant of richfaces-a4j.
  Unpacks the javax-based richfaces-a4j jar, transforms javax.* -> jakarta.*
  (Jakarta EE 9.1) and repackages it as richfaces-a4j-jakarta.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.github.luisch22.richfaces</groupId>
        <artifactId>richfaces-jakarta-bridge</artifactId>
        <version>5.0.0</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>richfaces-a4j-jakarta</artifactId>
    <packaging>jar</packaging>
    <name>RichFaces a4j (Jakarta EE 9.1)</name>

    <properties>
        <version.transformer.plugin>1.0.0</version.transformer.plugin>
        <maven.javadoc.skip>true</maven.javadoc.skip>
        <source.skip>true</source.skip>
        <gpg.skip>true</gpg.skip>
        <source.artifactId>richfaces-a4j</source.artifactId>
    </properties>

    <dependencies>
        <dependency>
            <groupId>com.github.luisch22.richfaces</groupId>
            <artifactId>${source.artifactId}</artifactId>
            <version>${version.richfaces}</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <executions>
                    <execution>
                        <id>unpack-source-jar</id>
                        <phase>process-classes</phase>
                        <goals>
                            <goal>unpack</goal>
                        </goals>
                        <configuration>
                            <artifactItems>
                                <artifactItem>
                                    <groupId>com.github.luisch22.richfaces</groupId>
                                    <artifactId>${source.artifactId}</artifactId>
                                    <version>${version.richfaces}</version>
                                    <outputDirectory>${project.build.outputDirectory}</outputDirectory>
                                    <excludes>META-INF/maven/**</excludes>
                                </artifactItem>
                            </artifactItems>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.eclipse.transformer</groupId>
                <artifactId>transformer-maven-plugin</artifactId>
                <version>${version.transformer.plugin}</version>
                <configuration>
                    <rules>
                        <jakartaDefaults>true</jakartaDefaults>
                    </rules>
                </configuration>
                <executions>
                    <execution>
                        <id>default-transform</id>
                        <phase>process-classes</phase>
                        <goals>
                            <goal>transform</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <executions>
                    <execution>
                        <id>default-jar</id>
                        <goals>
                            <goal>jar</goal>
                        </goals>
                        <configuration>
                            <archive>
                                <manifestFile>${project.build.outputDirectory}/META-INF/MANIFEST.MF</manifestFile>
                            </archive>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
