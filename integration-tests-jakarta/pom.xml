<?xml version="1.0" encoding="UTF-8"?>
<!--
  RichFaces Jakarta EE 10 integration smoke tests.

  A clean, modern integration harness for the Jakarta-native RichFaces library:
    - Arquillian 1.10.x (Jakarta) driving a managed WildFly 35 (Jakarta EE 10, JDK 21)
    - Selenium 4 with headless Chrome (Selenium Manager auto-provisions the driver)
    - ShrinkWrap 3.x to assemble the test WAR and resolve the richfaces-* jars

  Intentionally NOT part of the default reactor (WildFly is a ~200 MB download).
  Run explicitly with the `it-wildfly` profile:

    mvn -f integration-tests-jakarta/pom.xml -Pit-wildfly verify

  This replaces the legacy Graphene/PhantomJS/Selenium-2 stack (still present under
  build/build-resources, build/page-fragments and the components' src/test/integration)
  which targets JSF 2.x and is kept for incremental porting.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.github.luisch22.richfaces</groupId>
        <artifactId>richfaces-parent</artifactId>
        <version>5.0.0</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>richfaces-integration-tests-jakarta</artifactId>
    <packaging>jar</packaging>
    <name>RichFaces Integration Tests (Jakarta EE 10)</name>

    <properties>
        <maven.compiler.release>21</maven.compiler.release>
        <!-- This module produces no distributable artifact. -->
        <gpg.skip>true</gpg.skip>
        <maven.deploy.skip>true</maven.deploy.skip>
        <maven.install.skip>true</maven.install.skip>

        <!-- Modern Jakarta EE 10 integration stack -->
        <version.arquillian>1.10.2.Final</version.arquillian>
        <version.wildfly.arquillian>5.1.0.Final</version.wildfly.arquillian>
        <version.wildfly>35.0.0.Final</version.wildfly>
        <version.selenium>4.35.0</version.selenium>
        <version.shrinkwrap.resolver>3.3.7</version.shrinkwrap.resolver>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.jboss.arquillian</groupId>
                <artifactId>arquillian-bom</artifactId>
                <version>${version.arquillian}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.jboss.shrinkwrap.resolver</groupId>
                <artifactId>shrinkwrap-resolver-bom</artifactId>
                <version>${version.shrinkwrap.resolver}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.seleniumhq.selenium</groupId>
                <artifactId>selenium-bom</artifactId>
                <version>${version.selenium}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- Jakarta EE 10 web APIs (provided by WildFly at runtime) -->
        <dependency>
            <groupId>jakarta.platform</groupId>
            <artifactId>jakarta.jakartaee-web-api</artifactId>
            <version>10.0.0</version>
            <scope>provided</scope>
        </dependency>

        <!-- RichFaces Jakarta-native library under test -->
        <dependency>
            <groupId>com.github.luisch22.richfaces</groupId>
            <artifactId>richfaces-core</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.github.luisch22.richfaces</groupId>
            <artifactId>richfaces-a4j</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.github.luisch22.richfaces</groupId>
            <artifactId>richfaces</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- Test: Arquillian + JUnit 4 -->
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>4.13.2</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.jboss.arquillian.junit</groupId>
            <artifactId>arquillian-junit-container</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Test: ShrinkWrap Maven resolver to bundle the richfaces jars -->
        <dependency>
            <groupId>org.jboss.shrinkwrap.resolver</groupId>
            <artifactId>shrinkwrap-resolver-impl-maven</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Test: Selenium 4 (headless Chrome via Selenium Manager) -->
        <dependency>
            <groupId>org.seleniumhq.selenium</groupId>
            <artifactId>selenium-java</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <profiles>
        <!-- Managed WildFly 35 (Jakarta EE 10). Downloads and runs the server,
             deploys the WAR, and executes the ITs against real headless Chrome. -->
        <profile>
            <id>it-wildfly</id>
            <dependencies>
                <dependency>
                    <groupId>org.wildfly.arquillian</groupId>
                    <artifactId>wildfly-arquillian-container-managed</artifactId>
                    <version>${version.wildfly.arquillian}</version>
                    <scope>test</scope>
                </dependency>
            </dependencies>
            <build>
                <plugins>
                    <!-- Provision WildFly 35 into target/ before the tests. -->
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-dependency-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>unpack-wildfly</id>
                                <phase>pre-integration-test</phase>
                                <goals>
                                    <goal>unpack</goal>
                                </goals>
                                <configuration>
                                    <artifactItems>
                                        <artifactItem>
                                            <groupId>org.wildfly</groupId>
                                            <artifactId>wildfly-dist</artifactId>
                                            <version>${version.wildfly}</version>
                                            <type>zip</type>
                                            <overWrite>false</overWrite>
                                            <outputDirectory>${project.build.directory}</outputDirectory>
                                        </artifactItem>
                                    </artifactItems>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <!-- Run IT*.java in the integration-test phase (failsafe). -->
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-failsafe-plugin</artifactId>
                        <version>3.2.5</version>
                        <configuration>
                            <systemPropertyVariables>
                                <jboss.home>${project.build.directory}/wildfly-${version.wildfly}</jboss.home>
                                <arquillian.launch>wildfly-managed</arquillian.launch>
                            </systemPropertyVariables>
                        </configuration>
                        <executions>
                            <execution>
                                <goals>
                                    <goal>integration-test</goal>
                                    <goal>verify</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
